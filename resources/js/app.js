// import './bootstrap';
// import.meta.glob([
//     '../images/**',
//     '../fonts/**',
// ]);


window.addEventListener("load", (event) => {
    setTimeout(() => {
        document.querySelector(".loader").remove();
    },1000)
});